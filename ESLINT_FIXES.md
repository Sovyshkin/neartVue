# ESLint исправления - Миграция на Strapi API

## ✅ Исправленные ошибки ESLint

### Удалены неиспользуемые импорты axios из компонентов:

1. **AppArts.vue** - удален `import axios from "axios"`
2. **AppTickets.vue** - удален `import axios from "axios"`  
3. **BlockMiniMarket.vue** - удален `import axios from "axios"`
4. **CardOpen.vue** - удален `import axios from "axios"`
5. **CreateTicket.vue** - удален `import axios from "axios"`
6. **AppLogin.vue** - удален `import axios from "axios"`
7. **AppRegister.vue** - удален `import axios from "axios"`

### Обновлены методы аутентификации:

#### AppLogin.vue:
- ✅ Метод `login()` обновлен для использования `this.$api.auth.login()`
- ✅ Метод `verify()` отключен с уведомлением (2FA не поддерживается в базовом Strapi)
- ✅ Добавлено поле `password` в data()

#### AppRegister.vue:
- ✅ Метод `log()` обновлен для использования `this.$api.auth.register()`
- ✅ Метод `verify()` отключен с уведомлением (2FA не поддерживается в базовом Strapi)
- ✅ Добавлено поле `password` в data()

### Компоненты теперь используют централизованный API сервис:

Все обновленные компоненты используют `this.$api` вместо прямых вызовов axios:
- `this.$api.auth.login()` - аутентификация
- `this.$api.auth.register()` - регистрация
- `this.$api.artworks.getAll()` - получение картин
- `this.$api.artworks.getOne(id)` - получение одной картины
- `this.$api.tickets.create()` - создание тикета
- `this.$api.tickets.getAll()` - получение тикетов
- `this.$api.favorites.add()` - добавление в избранное
- `this.$api.cart.add()` - добавление в корзину

## 📋 Статус линтинга: ✅ УСПЕШНО

```bash
npm run lint
> DONE  No lint errors found!
```

## 🔧 Что изменилось в аутентификации:

### Старая система (Python backend):
- Отправка email → получение кода → ввод кода → аутентификация
- Двухэтапная аутентификация через SMS/email коды

### Новая система (Strapi):
- Стандартная JWT аутентификация: email + password → JWT token
- Токен сохраняется в localStorage
- Автоматическое добавление токена к запросам через interceptor

## ⚠️ Временно отключенные функции:

1. **Двухфакторная аутентификация (2FA)** - требует дополнительной настройки в Strapi
2. **Верификация по SMS/email** - не реализована в базовом Strapi

## 🚀 Готово к тестированию:

Все компоненты обновлены и готовы для работы с новым Strapi backend. ESLint проходит без ошибок.