# Статус миграции компонентов на Strapi API

## ✅ Полностью обновленные компоненты:

### Аутентификация
- `AppLogin.vue` - обновлен на `/api/auth/local`
- `AppRegister.vue` - обновлен на `/api/auth/local/register`

### Контент
- `AppArts.vue` - обновлен на `/api/artworks`
- `BlockMiniMarket.vue` - обновлен на `/api/artworks`
- `CardOpen.vue` - обновлен на `/api/artworks/[id]`

### Тикеты
- `CreateTicket.vue` - обновлен на `/api/tickets`
- `AppTickets.vue` - обновлен на `/api/tickets`

### Избранное и корзина
- `CardOpen.vue` - добавлены методы для `/api/favorites` и `/api/cart-items`

## 🔄 Требуют дополнительного обновления:

### Тикеты (частично)
- `AppTicket.vue` - нужно обновить получение и отправку ответов
  - Текущие endpoints: `/tickets/get?id=`, `/tickets/response/add`
  - Новые: `/api/tickets/[id]`, `/api/ticket-responses`

### Профиль пользователя
- `AppProfile.vue` - нужно обновить получение профиля
  - Текущий: `/get_user/[id]`
  - Новый: `/api/users/me`

### Платежи
- `MyPayments.vue` - нужно обновить
  - Текущий: `/billings/get/all`
  - Новый: `/api/payments`

### Корзина
- `AppCart.vue` - нужно полностью переписать
  - Текущие: `/cart/[id]/items`, `/orders/add`
  - Новые: `/api/cart-items`, `/api/orders`

### Избранное
- `AppFavorite.vue` - нужно обновить
  - Текущий: `/favorites/[id]`
  - Новый: `/api/favorites`

### Карточки товаров
- `ProductCard.vue` - нужно обновить удаление из корзины/избранного
  - Текущие: `/delete_elem`, `/favorites/remove`
  - Новые: `/api/cart-items` (DELETE), `/api/favorites` (DELETE)

### Обратная связь
- `FormFeedback.vue` - нужно обновить
  - Текущий: `/feedbacks/create`
  - Новый: `/api/feedbacks`

### Админка
- `AppAdmin.vue` - обновлен частично, но функционал перенесен в Strapi Admin

### Сброс пароля
- `ResetPassword.vue` - нужно обновить
  - Текущий: `/users/requireResetPassword`
  - Новый: `/api/auth/forgot-password`
- `СontrolReset.vue` - нужно обновить
  - Текущий: `/users/resetPassword`
  - Новый: `/api/auth/reset-password`

## 📝 Новые модели созданы в Strapi:

1. **Artist** - художники
2. **Artwork** - картины
3. **Ticket** - тикеты поддержки
4. **Ticket Response** - ответы на тикеты
5. **Order** - заказы
6. **Payment** - платежи
7. **Favorite** - избранное пользователей
8. **Cart Item** - элементы корзины
9. **Feedback** - обратная связь

## 🔧 Следующие шаги:

1. Завершить обновление всех компонентов из списка "Требуют дополнительного обновления"
2. Настроить правильные разрешения в Strapi Admin Panel
3. Протестировать все функции
4. Добавить обработку ошибок
5. Оптимизировать запросы с правильными populate параметрами